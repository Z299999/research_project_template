"""Stable kernel for the damped-oscillator sample experiment.

`oscillator.simulate(...)` integrates the model and returns a results dict;
`viz.fig_*` render figures from that dict. Neither changes between campaigns —
campaigns vary only the configuration passed in.
"""
