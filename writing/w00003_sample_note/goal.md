# Goal — w00003 sample note

A minimal, fully worked LaTeX note that demonstrates the writing workflow
end-to-end, as a companion to the experiment sample
`experiments/e00001_damped_oscillator`.

What it shows:

- a filled-in `main.tex` (title, abstract, one section) that actually builds;
- a display equation and a small `proposition` from `template.sty`;
- a **figure produced by the experiment** — `figures/oscillator_underdamped.png`
  was copied from `e00001/runs/latest/` (the c008 experiment→paper bridge);
- one real citation (`references.bib` + matching `literature/bibliography.jsonl`
  entry `b00002`), so the `\cite`/BibTeX loop is exercised.

This is intentionally tiny. Copy it for a real short note or report; copy
`w00001_project_name` instead for a full multi-section paper.
